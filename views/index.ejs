<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Index</title>
   <style>
      body *{
         margin: 3px;
      }
   </style>
</head>
<body>

   <h2>Welcome, These are the users that are in the database.</h2>

   <table>
      <tr><th>Name</th><th>Password</th></tr>
      <% for(let user of users){ %>
          <tr>
              <td><%= user.username %></td>
              <td><%= user.password %></td>
          </tr>
      <% } %>
  </table>

  <hr>

  <h3><%= msg %></h3>
  <h4><%= sessID %></h4>
  
</body>
</html>